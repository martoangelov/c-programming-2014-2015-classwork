#include<stdio.h>
#include<stdlib.h>
#include<time.h>

int main()
{
	int random_number = 0;
	int i;
	int castel[10];
	srand(time(NULL));
	for(i = 0; i<10; i++)
	{
		castel[i] = rand() % 9 + 1;
		printf("%d\n", castel[i]);
	}
	return 0;
}
